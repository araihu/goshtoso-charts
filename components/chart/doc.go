// Package chart defines renderer-neutral values shared by Goshtoso chart
// families.
//
// Chart-specific packages own their configurations and implementations. This
// package owns only values whose semantics are identical across those packages,
// including Instance, shared presentation options, and live snapshot shapes.
package chart
