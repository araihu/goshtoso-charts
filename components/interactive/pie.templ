package interactive

templ pieExactValues(rows []pieValueRow) {
	<details class="mt-3 max-w-full text-sm text-on-surface dark:text-on-surface-dark">
		<summary class="cursor-pointer font-semibold">Exact pie values</summary>
		<div class="mt-2 overflow-x-auto rounded-radius border border-outline dark:border-outline-dark">
			<table class="min-w-full border-collapse text-left">
				<thead>
					<tr>
						<th class="px-3 py-2" scope="col">Series</th>
						<th class="px-3 py-2" scope="col">Sector</th>
						<th class="px-3 py-2" scope="col">Value</th>
						<th class="px-3 py-2" scope="col">Share</th>
					</tr>
				</thead>
				<tbody>
					for _, row := range rows {
						<tr class="border-t border-outline dark:border-outline-dark">
							<th class="px-3 py-2 font-semibold" scope="row">{ row.Series }</th>
							<td class="px-3 py-2">{ row.Name }</td>
							<td class="px-3 py-2 tabular-nums">{ row.Value }</td>
							<td class="px-3 py-2 tabular-nums">{ row.Share }</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</details>
}
